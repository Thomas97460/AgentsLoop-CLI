source_up

# Clear any inherited VIRTUAL_ENV that doesn't match our project's .venv
# to avoid uv mismatch warnings.
if [[ -n "$VIRTUAL_ENV" && "$VIRTUAL_ENV" != "$(pwd)/.venv" ]]; then
  unset VIRTUAL_ENV
fi

use flake
